import tkinter as tk
from tkinter import messagebox

def open_window_manchine():
    window_manchine = tk.Toplevel
    window_manchine = title("atm")
    window_manchine = geometry("350x500")

    screen = tk.label(window_manchine)

    Screen.pack(pady=10)